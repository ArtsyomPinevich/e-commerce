import React from 'react';
import SideBar from '../SideBar/SideBar';
import './Main.scss';

const Main = () => {
    return (
        <section className="hero">
            <SideBar />
        </section>
    );
};

export default Main;
